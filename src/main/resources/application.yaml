spring:
  application:
    name: Nae-Il
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/naeil_db
    username: naeil
    password: naeil
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true
  servlet:
    multipart:
      max-file-size: 500MB
      max-request-size: 2GB

file:
 upload-dir: ./uploads/
